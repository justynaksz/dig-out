import React from 'react';

export type SignInRequestType = {
    nickname: string;
    password: string;
}

export const SignInRequest = ({
    nickname,
    password} : SignInRequestType) => {
        return (
            <>
            </>
        )
    }